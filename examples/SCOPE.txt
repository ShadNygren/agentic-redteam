# Copy to /work/SCOPE.txt. One in-scope target per line (host, domain, CIDR, or URL).
# Lines starting with # are comments. Anything NOT listed here is out of scope and
# the scope guard will refuse it. Keep this file in sync with the signed AUTHORIZATION.md.
#
# Examples (delete these and add the real, authorized in-scope targets):
# 192.0.2.10
# 198.51.100.0/24
# app.staging.example.com
# https://staging.example.com/
